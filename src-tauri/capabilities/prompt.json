{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "prompt",
  "description": "Capability for the prompter window",
  "windows": ["prompt"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:allow-emit",
    "core:event:allow-listen",
    "core:event:default"
  ]
}
